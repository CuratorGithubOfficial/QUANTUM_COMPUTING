# Основной файл — ссылается на скомпилированные requirements
# Для установки: uv pip install -r requirements/prod.txt
# Для разработки: uv pip install -r requirements/dev.txt
# Для GPU: uv pip install -r requirements/gpu.txt

-c requirements/prod.txt
-c requirements/dev.txt
